ROLE
You are Claude Code working in the mini-commerce engineering playground. Work
in English. Do not add AI attribution, generated-by text, co-author trailers,
or tool ownership claims.

GOAL
Continue from the latest manual UAT results and produce an effort estimate,
tracking plan, and implementation-ready remediation backlog for all pending
items detected in the UAT run. Do not start by coding. First inspect the
referenced files, confirm the current state against the code, estimate effort,
and produce a prioritized remediation plan.

READ FIRST
- CLAUDE.md
- docs/README.md
- docs/ai/codex/governance.md
- docs/ai/codex/current-findings.md
- docs/uat/web-app-uat.md
- docs/project-state/current-system.md
- docs/architecture/web-entry-point.md
- docs/local-development.md

CONFIRM IN CODE BEFORE ESTIMATING
- apps/web/app/orders/[orderId]/page.tsx
- scripts/playground.mjs
- apps/web/app/visualizer/page.tsx
- apps/visualizer-3d/public/scene.js
- apps/bff/src/modules/visualization/visualization.service.ts
- infra/docker/compose.yaml

KNOWN UAT FINDINGS TO START FROM
1. FAIL: `/orders/ord_demo` returned 500 instead of rendering an order detail
   page.
2. FAIL: `/orders/does-not-exist` returned 500 instead of a graceful
   order-not-found state.
3. DRIFT: `pnpm pg:up full` migrated and seeded, then failed with
   `unknown flag: --profile` on the tested Docker Compose version.
4. SKIP: header/footer navigation, cart drawer pixels, Demo Mode scenario
   pixels, `/dev` card interactions, and 3D scene painting/color/reload
   reactivity were not browser-verified because no browser automation surface
   was available.

FUNCTIONAL BOUNDARIES
- Preserve the web app as the single browser-facing entry point.
- Preserve `/api/bff/*` and `/viz/*` proxy topology.
- Preserve persistent orders and `GET /orders`.
- Preserve the in-memory nature of the cart.
- Preserve typed frontend API usage.
- Keep `/performance` mock-only; do not claim live telemetry, Grafana data, or
  k6 execution.
- Do not modify backend contracts, telemetry, k6 scenarios, or visualizer
  internals unless the owner explicitly approves that scope.
- Visualizer embed glue may be assessed, but do not change backend contracts or
  visualizer internals for this effort-estimation task.

TASKS
1. Reconstruct the UAT failure/drift/skip list from `docs/uat/web-app-uat.md`.
2. Confirm each pending item against the current code and, where possible,
   against local runtime commands.
3. Group pending items into remediation tracks:
   - Critical user-flow blockers.
   - Runtime/developer workflow drift.
   - Manual browser verification gaps.
   - Optional visualizer follow-ups.
4. Estimate effort for each item using:
   - Size: XS, S, M, L.
   - Confidence: High, Medium, Low.
   - Estimated range: hours or half-days.
   - Risk: low, medium, high.
   - Dependencies or approval gates.
5. Identify the minimal fix path for each item and the exact files likely to
   change.
6. Define validation for each item:
   - Shell checks.
   - Browser/manual checks.
   - Typecheck/build/smoke commands where relevant.
7. Create or update a durable remediation tracker in
   `docs/next-steps/uat-remediation.md` unless a better existing next-steps
   document already owns the work.

EXPECTED TRACKER FORMAT
Use a concise table with these columns:
- ID
- UAT source
- Problem
- User impact
- Likely cause
- Proposed fix
- Files
- Estimate
- Confidence
- Risk
- Validation
- Status

Use statuses:
- pending
- in_progress
- blocked
- done
- deferred

PRIORITIZATION RULES
- P0: Blocks checkout confirmation, order detail, order status management, or
  invalid-order resilience.
- P1: Blocks documented local development workflow.
- P2: Blocks confidence in visual UX but not known functionality.
- P3: Optional polish or future integration.

OUTPUT
Before editing code, report:
1. A short summary of confirmed pending items.
2. The remediation tracker path.
3. The effort estimate table.
4. The recommended implementation order.
5. Any approval gates.

Then stop unless explicitly asked to implement fixes.

VALIDATION COMMANDS TO CONSIDER
- pnpm pg:smoke
- curl -s -o /dev/null -w '%{http_code}' http://localhost:3000/orders/ord_demo
- curl -s -o /dev/null -w '%{http_code}' http://localhost:3000/orders/does-not-exist
- curl -s -o /dev/null -w '%{http_code}' http://localhost:3000/api/bff/health
- curl -s -o /dev/null -w '%{http_code}' http://localhost:3000/viz/index.html
- curl -s -o /dev/null -w '%{http_code}' http://localhost:3000/viz/scene.js
- pnpm typecheck
- pnpm build

IMPORTANT
This prompt is for estimation and tracking first. Do not implement fixes in the
same pass unless the owner explicitly asks for implementation after reviewing
the remediation tracker.
