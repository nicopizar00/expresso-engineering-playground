{
  "name": "@travel-playground/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "API and event contracts (OpenAPI, JSON Schema, Pact pacts) for the travel booking playground.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo 'TODO: wire tsc and contract generation'",
    "lint": "echo 'TODO: wire eslint'",
    "typecheck": "echo 'TODO: wire tsc --noEmit'",
    "test": "echo 'TODO: wire contract self-tests'"
  }
}
