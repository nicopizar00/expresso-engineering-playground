{
  "name": "@mini-commerce/config",
  "version": "0.0.0",
  "private": true,
  "description": "Shared configuration (eslint, tsconfig presets, prettier) for the mini-commerce playground.",
  "scripts": {
    "lint": "echo 'TODO: self-lint config files'"
  }
}
