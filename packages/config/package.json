{
  "name": "@travel-playground/config",
  "version": "0.0.0",
  "private": true,
  "description": "Shared configuration (eslint, tsconfig presets, prettier) for the travel booking playground.",
  "scripts": {
    "lint": "echo 'TODO: self-lint config files'"
  }
}
