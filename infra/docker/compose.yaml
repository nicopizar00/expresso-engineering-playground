# Local development stack for the travel booking playground.
#
# This file is a PLACEHOLDER. It declares the services the local stack will
# eventually run, but with minimal config and no secrets. Production
# deployment configuration does NOT live here.
#
# Bring up locally (next iteration):
#   docker compose -f infra/docker/compose.yaml up
#
# Conventions:
#   - All credentials are sourced from a local .env file (gitignored).
#   - No real URLs, image tags, or company names are committed.
#   - Versions are pinned conservatively to public, well-known images.

services:
  postgres:
    image: postgres:16-alpine
    environment:
      # TODO: source these from a local .env file. Defaults below are
      # intentionally weak placeholders for local dev only.
      POSTGRES_USER: playground
      POSTGRES_PASSWORD: playground
      POSTGRES_DB: travel_playground
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

  otel-collector:
    # TODO: switch to the contrib image once a concrete pipeline is defined.
    image: otel/opentelemetry-collector:latest
    command: ["--config=/etc/otel-collector-config.yaml"]
    volumes:
      - ../observability/otel-collector-config.yaml:/etc/otel-collector-config.yaml:ro
    ports:
      - "4317:4317"  # OTLP gRPC
      - "4318:4318"  # OTLP HTTP

  # TODO (future):
  #   - bff:    build context apps/bff, depends_on postgres + otel-collector
  #   - web:    build context apps/web, depends_on bff
  #   - prometheus / grafana / tempo / loki for the observability stack

volumes:
  postgres-data:
