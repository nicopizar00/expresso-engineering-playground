"""pg — developer orchestrator CLI for the mini-commerce playground.

Public surface is `scripts/pg/__main__.py` (invoked via `python3 -m pg` from
the `scripts/` directory by the `./dev` trampoline). Importable submodules
exist for tests under `scripts/pg/tests/`.
"""
