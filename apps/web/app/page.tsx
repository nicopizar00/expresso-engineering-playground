// TODO: Replace with the real Next.js landing page once the app is wired.
// Kept as a typed placeholder so the workspace has a buildable entry point.

export default function HomePage(): JSX.Element {
  return (
    <main>
      <h1>Travel Booking Playground</h1>
      <p>Web app placeholder. See apps/web/README.md.</p>
    </main>
  );
}
