generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Product {
  id               Int      @id @default(autoincrement())
  productId        String   @unique
  sku              String   @unique
  name             String
  description      String
  category         String
  priceAmountMinor Int
  priceCurrency    String
  inventory        Int
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt
}

// TODO(next-steps/orders-persistence): add Order + OrderLine models here. OrderLine FK to Order on orderId; OrderLine references Product by productId (no FK — keep loose for now). See docs/next-steps/orders-persistence.md
